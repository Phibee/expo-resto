import React from 'react';
import { StyleSheet, Text, View, ScrollView } from 'react-native';

export const BrancheScreen = ({ navigation }) => {
	return (
		<View style={{ backgroundColor: 'white', flex: 1, padding: 15 }}>
			<Text>Branches</Text>
		</View>
	);
};

export default BrancheScreen;
